"""Extraction — ASR + claim extractor.

Owned by `wt-extraction` (see CLAUDE.md §5.2):
- `asr/` — VAD + faster-whisper + WhisperX alignment + pyannote diarisation
- `claim_extractor/` — Opus 4.7 prompt + validation (Phase 2, blocked on wt-engine API)
"""
