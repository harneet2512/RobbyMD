"""Flow Variant A — Whisper-large-v3-turbo + WhisperX + pyannote + BioMistral cleanup.

Measurement-only module. Imports lazily so that tests and tooling can grep the
package without pulling torch / vllm.
"""
